# Minimal client

The `home/` tree contains the complete client payload. A real repository would
track `home/.bashrc` as `.bashrc` in its `$HOME` work tree; the extra directory
exists here only to keep this example self-contained.
