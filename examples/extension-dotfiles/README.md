# Extension client

This example enables extension API 1 and points at a separate client-owned
library containing one merge hook and one doctor extension. Dot discovers only
documented collections below that library.
